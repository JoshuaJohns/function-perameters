const quarterValue = (originalNumber) => {
        let newNumber = originalNumber / 4
    return newNumber
}

// Invoke the function and store the return value
const result = quarterValue(16)

// Log the return value
console.log(result)